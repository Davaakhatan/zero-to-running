apiVersion: v1
kind: Secret
metadata:
  name: dev-env-secrets
  namespace: dev-env
type: Opaque
stringData:
  # Database Password
  POSTGRES_PASSWORD: "devpass"
  
  # Database Connection String (will be constructed from secrets)
  DATABASE_URL: "postgresql://devuser:devpass@postgres-service:5432/devenv"
  
  # Redis Connection String
  REDIS_URL: "redis://redis-service:6379"

---
# Note: In production, use Kubernetes Secrets Manager or Azure Key Vault
# This is a template - replace with actual secrets management

