services:
  app-frontend:
    port: 3000
    host: localhost
  
  backend:
    port: 3003
    host: localhost
  
  database:
    host: localhost
    port: 5432
    name: devenv
    user: devuser
  
  redis:
    host: localhost
    port: 6379

healthChecks:
  interval: 30
  timeout: 5000

