// Enum-related type definitions

export enum ShapeType {
  RECTANGLE = 'rectangle',
  CIRCLE = 'circle',
  TRIANGLE = 'triangle',
  TEXT = 'text',
  ELLIPSE = 'ellipse',
  STAR = 'star',
  POLYGON = 'polygon',
  PATH = 'path',
  IMAGE = 'image'
}

// Removed duplicate enums that are already defined in projectTypes.ts
// to avoid ambiguous star export conflicts
